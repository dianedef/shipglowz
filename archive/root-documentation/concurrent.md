# Legacy Compatibility Facade

This root `concurrent.md` is no longer the canonical competitor/inspiration note.

Use `shipglowz_data/workflow/concurrent.md` as the current project-local location:

- canonical file: `shipglowz_data/workflow/concurrent.md`
- why this file still exists: compatibility fallback during layout cleanup
- rule: update the canonical file only
