---
interface Props {
  items: [string, string][];
}

const { items } = Astro.props;
---

<div class="faq-list">
  {items.map(([q, a]) => (
    <details class="faq-item">
      <summary>{q}</summary>
      <p>{a}</p>
    </details>
  ))}
</div>
