---
import { sharedHomeSections, type Locale } from "../i18n/ui";
import SectionHeading from "./SectionHeading.astro";
import PillGrid from "./PillGrid.astro";

interface Props {
  locale?: Locale;
}

const { locale = "en" } = Astro.props;
const copy = sharedHomeSections[locale].proof;
---

<section class="section">
  <SectionHeading eyebrow={copy.eyebrow} title={copy.title} body={copy.body} />
  <PillGrid items={copy.pills} />
</section>
