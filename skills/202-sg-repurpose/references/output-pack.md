# Source-Faithful Pack

Load `$SHIPFLOW_ROOT/skills/references/source-faithful-pack-contract.md` first. That shared reference is the canonical structure, section order, and compression doctrine.

This local reference only adds `202-sg-repurpose`-specific narrowing.

Guidance:

- The first deliverable remains the shared source-faithful pack, not free paragraphs and not a surface-specific draft by default.
- `Owner Skill Handoffs` must follow `$SHIPFLOW_ROOT/skills/references/content-owner-handoffs.md`; `202-sg-repurpose` does not write the content itself.
- If no blog or article surface is declared, keep article ideas and titles only when still useful, mark destination as `surface missing: blog`, and do not invent paths.
- `Supporting Source Notes` should stay in appendix position unless the user explicitly asks for a detailed evidence report.
- `Diffusion Map` is mandatory when the source idea is site-facing or SEO-relevant.
- `Handoff Checklist` is mandatory when the user asks to apply, create, update, or fill site/docs surfaces.
- When the user asks to apply or write, expand `Owner Skill Handoffs` and `Handoff Checklist`, but do not claim files were changed by `202-sg-repurpose`.
