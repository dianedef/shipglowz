{
  "name": "shipglowz-terminal-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Optional read-only ShipGlowz terminal dashboard (Bun + OpenTUI)",
  "engines": {
    "node": ">=22.12.0 <25"
  },
  "scripts": {
    "dev": "bun run src/main.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo 'lint not configured yet'"
  },
  "dependencies": {
    "@opentui/core": "0.2.14"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "^5.6.3"
  }
}
